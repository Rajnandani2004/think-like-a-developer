#include<bits/stdc++.h>
using namespace std;
int compar(int a, int b){
     int i;
     for(i=7;i>=0;i--){
        if(a[i]>b[i]){
            return 1;
            break;
        }
        else
        return 0;
     }
}
int main(){
    int a[8],b[8];
    for(int i=0;i<8;i++){
        cin>>a[i];
    }
    for(int i=0;i<8;i++){
        cin>>b[i];
    }
    if(compar(a[8],b[8])==1)
    cout<<"b is elder than a"<<endl;
    else
    cout<<"b is younger than a"<<endl;
    return 0;
}